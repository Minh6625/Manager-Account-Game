# API base URL (no trailing slash)
# Local: http://localhost:3000
# Same Vercel project as API: leave unset; production falls back to same-origin /api
# Separate API domain: https://your-api.example.com
VITE_API_URL=http://localhost:3000
